'''
.：万能字符（只能1个任意字符）
[]：字符簇，匹配[]范围内的任意某个字符（1次只能匹配1个）
[^]：字符簇，匹配除[]范围以外的其他任一字符
\d：数字
\D：非数字
\w：0-9,a-z,A-Z,_
\W：非\w以外其他字符
\s：空白字符
\S：非空白字符

注意：匹配符默认一次只能匹配1个字符
'''
import re

# 1. 万能字符
# result = re.match('.', '_123')
# 2. 匹配a、e、i、o、u中的任意某个字符
# result = re.match('[aeiou]', 'ab123')
# 3. 匹配除a-z以外的其他字符
# result = re.match('[^a-z]', '91234')
# 4. 匹配非空白字符
# result = re.match('\S', 'X1234')
# 5. 匹配数字字符
result = re.match('\d', '12345')
print(result.group())
